<fieldset class: 'sponsor_fields'>

	<%= render 'person_fields', f: f %>

	<%= f.fields_for :yizkors do |yizkor| %>
		<fieldset>
			<%= render 'person_fields', f: yizkor %>		
		</fieldset>
	<% end %>

</fieldset>
